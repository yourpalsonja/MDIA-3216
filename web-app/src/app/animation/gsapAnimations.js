import styles from "./gsap.module.css";

export default function GSAPAnimations() {
    return (
        <section className={styles.cssSection}>
            <span className={styles.confetti}>🎊</span>
            <span className={styles.heart}>🩵</span>
            <span className={styles.lettuce}>🥬</span>
        </section>
    );
}